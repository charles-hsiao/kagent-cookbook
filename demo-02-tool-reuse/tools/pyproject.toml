[project]
name = "shared-utils-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.3.0",
]

[tool.setuptools]
py-modules = ["server"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
